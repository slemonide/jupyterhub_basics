version: '3.8'

services:
  jupyterhub_test:
    build: hub_image
    image: phaustin/hub07:sep23
    container_name: jupyterhub_sep23n7
    depends_on: [notebook]
    ports:
      - ${NB_PORT}:8000
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
    networks:
      - net_test

  notebook:
    image: phaustin/notebook:sep23n7
    build:
      context: notebook_image
    container_name: notebook_sep23n7

# force the network name to be "jupyterhub"
# this needs to be matched in jupyterhub_config.py by:
# c.DockerSpawner.network_name = 'jupyterhub'
networks:
    net_test:
      driver: bridge
      name: net_sep23n7

